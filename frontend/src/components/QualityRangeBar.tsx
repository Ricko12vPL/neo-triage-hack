import type { AstrometricQualityBreakdown } from "../api/types";

/**
 * Visual A→F spectrum range bar with a marker.
 *
 * (Stub — full implementation lands in BLOCK_6.)
 */

interface Props {
  breakdown: AstrometricQualityBreakdown;
}

export function QualityRangeBar({ breakdown: _breakdown }: Props) {
  return null;
}
